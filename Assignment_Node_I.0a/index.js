// Write a module containing af function that returns true if all provided parameters have values, and false otherwise.
const myModule = require('./myModule'); // import function from another script

// log to test if true
console.log(myModule.bool(1,2));

// log to test if true
console.log(myModule.bool(1));